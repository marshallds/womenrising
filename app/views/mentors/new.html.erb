<div class="container">
	<h1 class="subject">Request A Mentor</h1>
	<div class="mentor">
		<h3>What would you like to talk with your mentor about?</h3>
		<p>Some examples from last round include: "How do I ask for a raise?", "How can I make time for both my significant other while being so busy?" or "How can I get my first job as a developer?"</p>
		<%= form_for @mentor do |f| %>
		  <% if @mentor.errors.messages != {} %>
		    <h3>You have errors:</h3>
		    <% @mentor.errors.messages.each_with_index do |message, index| %>
		      <li>
		      	<h4><%= message[0] %></h4>
		      	<p><%= message[1][0] %></p>
		      </li>
		    <% end %>
		  <% end %>
		  <div class="form-group">
			  <%= f.text_field :question, class: 'form-control' %>
		  </div>
		  <div class="form-group">
			  <%= f.submit "Submit", class: "btn" %>
		  </div>
		<% end %>
	</div>
</div>
